version: "3"
services:
  jenkins-setup:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "50000:50000"
      - "8080:8080"
    volumes:
      - ~/jenkins_home:/var/jenkins_home
